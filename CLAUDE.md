# CLAUDE.md

This file provides guidance to Claude Code when working in this repository.

## Run

```bash
node index.js
```

## Architecture

TBD — fill in as the project takes shape.

## Env vars required

See `.env.example`. Copy to `.env` and fill in real values (never commit `.env`).
